name: sast-tools

on:
  push:
    branches:
      - main
  workflow_dispatch: 

permissions:
  contents: read

jobs:
  bearer:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Report
        id: report
        uses: bearer/bearer-action@v2
        with: 
          exit-code: 0
  poutine:
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
    steps:
      - name: poutine - GitHub Actions SAST
        uses: boostsecurityio/poutine-action@f350a41901f0f06fa964b65b72a7ab63b644c5b2
      - name: Upload poutine SARIF file
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif
